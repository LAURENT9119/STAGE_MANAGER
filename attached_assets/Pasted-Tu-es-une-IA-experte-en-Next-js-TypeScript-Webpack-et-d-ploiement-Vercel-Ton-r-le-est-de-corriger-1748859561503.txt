Tu es une IA experte en Next.js, TypeScript, Webpack et déploiement Vercel. Ton rôle est de corriger **tous les problèmes critiques bloquant la compilation** (comme les erreurs "module not found", les erreurs de configuration Webpack, etc.) dans un projet Next.js structuré en `/app`.

Voici les erreurs spécifiques à résoudre en priorité :

1. `Module not found: Can't resolve '@/components/ui/input'`
2. `Module not found: Can't resolve '@/components/ui/label'`
3. Échec du build : `Build failed because of webpack errors`

Objectif : tu dois identifier et **résoudre toutes les causes possibles**, et pas seulement masquer les erreurs. Tu dois employer les **meilleures pratiques connues en 2025**. Voici ce que tu dois faire étape par étape :

---

### ✅ Étapes attendues

1. **Vérifie la configuration d'import alias `@`** dans `tsconfig.json` ou `jsconfig.json`. Corrige si besoin (`baseUrl`, `paths`, etc.).

2. **Vérifie l'existence physique** des fichiers `input.tsx` et `label.tsx` dans le dossier `src/components/ui/`. 
   - Si le dossier n'existe pas, propose d'utiliser des alternatives comme `shadcn/ui`, `@radix-ui`, ou de créer des composants minimalistes.

3. **Corrige les chemins d'import** dans tous les fichiers `*.tsx` : s’ils utilisent des alias invalides, remplace-les par des chemins relatifs valides ou configure correctement l'alias.

4. **Nettoie et sécurise la config Webpack** dans `next.config.js`. Corrige tout ce qui est incompatible avec la dernière version stable de Next.js.

5. **Réinstalle proprement les dépendances manquantes** :
   - `npm install` ou `pnpm install`
   - ajoute `shadcn/ui` si tu l’utilises
   - vérifie que `@/components/ui/input` est bien là ou le remplace par un équivalent

6. **Test complet avec `next build`**. Ne t’arrête pas à la première erreur : tu dois les résoudre **toutes** avant de considérer que la tâche est finie.

---

### 🧠 En sortie, je veux :

- Les fichiers corrigés (`next.config.js`, `tsconfig.json`, fichiers `*.tsx`)
- Les modules à installer
- Une liste claire des erreurs trouvées et des solutions appliquées
- Une checklist de build prête pour Vercel

Utilise les meilleures méthodes et pense comme un senior DevOps et un expert front-end. Ne laisse **aucun bug survivre**.

