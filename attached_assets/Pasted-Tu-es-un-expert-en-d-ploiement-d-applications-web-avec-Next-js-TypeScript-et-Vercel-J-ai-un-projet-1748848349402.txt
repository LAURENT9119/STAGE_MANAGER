Tu es un expert en déploiement d’applications web avec Next.js, TypeScript et Vercel. J’ai un projet que je souhaite déployer sur Vercel, mais j’obtiens des erreurs de build critiques. Je veux que tu agisses comme un auditeur et réparateur de build en profondeur.

Voici ce que je veux que tu fasses :

1. **Analyse l’intégralité du code fourni** (y compris les fichiers `.tsx`, `package.json`, `next.config.js`, et tous les composants si nécessaires).
2. **Corrige toutes les erreurs de syntaxe** TypeScript, JSX et Next.js : erreurs de balises JSX mal fermées, fonctions non terminées, code en dehors de `return()`, etc.
3. **Répare toute mauvaise structure de code** dans les pages comme `page.tsx` qui bloquent le build, même si ce sont des erreurs silencieuses.
4. **Mets à jour toutes les dépendances dépréciées** et propose des alternatives actuelles et maintenues. Remplace les packages obsolètes automatiquement si tu le peux.
5. **Corrige tous les fichiers de configuration** (comme `package.json`, `tsconfig.json`, `next.config.js`, `vercel.json`) pour qu’ils soient valides, à jour, et compatibles avec Next.js 13+ et Vercel.
6. **Gère tous les problèmes de compatibilité entre versions de Next.js, React et les packages NPM** (même ceux qui ne déclenchent pas une erreur immédiate mais bloquent indirectement le build).
7. **Supprime tout code mort ou résiduel** dans le projet qui pourrait causer des erreurs de compilation.
8. **Vérifie que la commande `npm run build` fonctionne localement** et en environnement Vercel.
9. **Donne-moi une version corrigée, propre, optimisée, prête à déployer**, avec le minimum de warning et zéro erreur.
10. **Agis comme si tu étais un auditeur de build en production : soit proactif, rigoureux et précis.**

Je veux que tu traites chaque fichier qui cause une erreur comme une priorité. Si tu identifies une mauvaise pratique, corrige-la. Ne me donne pas seulement des conseils : **réécris le code corrigé**.

Je peux te fournir les fichiers suivants si tu veux les corriger :
- `package.json`
- `page.tsx` ou autres fichiers en erreur
- Configurations

Tu peux commencer en me demandant ce qu’il te manque pour tout réparer.

